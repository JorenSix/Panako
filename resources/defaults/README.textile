h1. Panako - Defaults

This directory contains the default configuration for panako.

* @config.properties@ contains default algorithm configuration
* @logging.properties@ contains default logging configuration
* @panako@ is the default startup bash script
 
